{
  "name": "@etl/profiling",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@etl/schema-model": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "//": "DuckDB (duckdb-async) is added in the worker where profiling runs on large files; the pure-JS profiler here handles samples and keeps this package light."
}
